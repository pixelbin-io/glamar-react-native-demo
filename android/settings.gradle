pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    // 👇 This tells Gradle to look inside node_modules
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

plugins {
    // 👇 No version here — resolved from node_modules
    id("com.facebook.react.settings")
}

rootProject.name = "glam"
include(":app")
